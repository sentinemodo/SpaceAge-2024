using System;
using System.Collections.Generic;
using System.Text;

namespace SpaceAge
{
	public enum EOrderType
	{
		active,
		alias,
		attack,
		buy,
        copy,
		form, 
		get,
		give,
		has,
		move,
		name,
        produce,
		research,
		set,
		see,
        sell,
		stack,
        transfer,
		train,
        use
	}
}
