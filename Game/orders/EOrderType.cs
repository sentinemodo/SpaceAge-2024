using System;
using System.Collections.Generic;
using System.Text;

namespace SpaceAge
{
	public enum EOrderType
	{
		activate,
		active,
		alias,
		attack,
		capture,
		buy,
        copy,
		contract,
		declare,
		deactivate,
		deposit,
		form, 
		get,
		give,
		has,
		jump,
		move,
		name,
		press,
		rumor,
		produce,
		repair,
		research,
		set,
		see,
        sell,
		stack,
		tactic,
        transfer,
		train,
        use,
		withdraw
	}
}
