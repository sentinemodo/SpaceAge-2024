using System;
using System.Collections.Generic;
using System.Text;

namespace SpaceAge
{
	public enum EOrderType
	{
		active,
		alias,
		attack,
		capture,
		buy,
        copy,
		contract,
		declare,
		deposit,
		form, 
		get,
		give,
		has,
		jump,
		move,
		name,
		press,
		produce,
		repair,
		research,
		set,
		see,
        sell,
		stack,
		tactic,
        transfer,
		train,
        use,
		withdraw
	}
}
