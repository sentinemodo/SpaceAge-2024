using System;
using System.Collections.Generic;
using System.Text;

namespace SpaceAge
{
	public enum EOrderType
	{
		active,
		alias,
		attack,
		capture,
		buy,
        copy,
		contract,
		declare,
		form, 
		get,
		give,
		has,
		move,
		name,
        produce,
		repair,
		research,
		set,
		see,
        sell,
		stack,
		tactic,
        transfer,
		train,
        use
	}
}
