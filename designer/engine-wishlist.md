# Engine wishlist (game designer → TDD)

Designer does **not** implement these. TDD adds a failing test first. Campaign XML stays loadable without them.

| Need | Objective | Suggested surface |
|------|-----------|-------------------|
| Moon constructed with moon `@name`, not planet name | Unique moon ids (`M00001`) survive load | `DataFile.LoadGalaxy` moon constructor |
| `loadGalaxyExits` walks moon regions and orbits | Moon maps and orbit↔surface space hops. Campaign currently uses **planet-region ↔ planet-region** space exits (Arbor/Anvil spaceports ↔ local dust/belt ↔ the pair hop) because those load today | Second pass over all `Region` / `Orbit` |
| Space transit time from ΔAU × drive `speed` | L10 ark crosses a system in weeks; L0 shuttle does not | `Moving` / exit duration formula; keep `AU` on planets |
| System `X Y Z` loaded | Interstellar placement (still no FTL) | Uncomment coordinate assign |
| Module group `capital` (or `ark`) | L10 hull is not a “frigate” | `EModuleTypesGroup` + `getModuleTypeGroup` |
| Contract triggers: `survive-weeks`, `destroy-stack`, `region-resource-below` | Bombardment, fauna, pirate hunt | `IContractTrigger` + XML attrs |
| `Events` pipeline for timed spawns | Alien reactivation, impact week | `Game/Events.cs` (today stub) |
| Hostility flip on module-build trigger | Arbor First (Arbor) and HCS (Anvil) start neutral, then flip hostile when first spaceship module is completed on their bound planet | Build-completion hook checks module group/tag and planet scope; sets faction diplomacy state and emits report event |
| NPC raid scheduler by tier | Medium-pressure cadence for turns 1-20 with staged wave size, composition, and delay | `Events`/GM scheduler: spawn templates by turn-band, lane, and active hostility state |
| Non-lethal raid targeting policy | Threats pressure convoys/industry first and avoid instant faction deletion | Target-priority resolver and damage caps: prefer outlying stacks/modules before HQ core stack; enforce one-wave HQ safety ceiling |
| Fauna/rogue AI guard behavior | Exploration combat at L0-L2 around resource clues and abandoned industry nodes | Guard stance + proximity aggro + site ownership lock until defeated; optional retreat/hibernate state after failed attack |
| `use-produce effect` execution | Catalog `repair` and future ECLSS effects | `UseOrder` / `EProductionType.Effects` |
| Skill children (`usable-in`, cure-chance) applied in battle/medical | Officers matter on arks and labs | `SkillType` fill-pass |
| `requires-tech` attr on skill entry | Skill rank only appears in training menu (and player report "Known Skills") after the prerequisite tech is researched. Mirrors technology visibility | `SkillType` loader reads `requires-tech`; report generator emits "Known Skills" section (duplicate tech-report logic) |
| `replaces-skill` attr on skill entry | When officer completes training in a higher rank, the old rank is removed. e.g. `engrA` replaces `engrB`. Prevents stacking | `TrainOrder` completion removes `replaces-skill` from person |
| Percentage `<produce>` on skills | `<produce effect="build rate" target="stacked" value="0.25"/>` means +25% multiplier to the relevant stat of the commanded stack. Engine must interpret skill produce values as multiplicative bonuses, not additive flat values | `SkillType` produce → `ModuleStack` stat calculation applies `(1 + sum_of_skill_values)` multiplier |
| Skill report in player turn report | "Known Skills" section lists all skills unlocked (via `requires-tech` satisfied or no gate). Same format as "Known Technologies". Player sees which ranks are available for training | Report generator: iterate skill catalog, filter by `requires-tech` ∈ faction known techs, emit list |
| Training slots = module count, not speed | Each research module on a stack provides 1 training slot (trains 1 officer at a time). Multiple modules = parallel trainees but each still takes full `training-duration` weeks. Different from production stacking which increases throughput | `TrainOrder` allocates officer to a free research module. Module busy flag for `training-duration` weeks. No duration reduction from extra modules |
| Item `radiation` / equipment bonuses in combat and vacuum | Vests, suits, dosimeters | `ItemType` + consume/medical |
| New `location-type` `atmosphere` | Gas-giant cloud regions | `LoadLocationType` |
| Asteroid belt as first-class or typed planet exits | Belt drift between rocks. **Note:** in-place extraction on belt regions already works via L0–L2 `cdrill`/`iminng`/`tminng`/`uminng`/`cminng` on any solid surface including `abelt` regions — the blocker is reaching them (no intra-belt exits today) | planet `abelt` already; optional space exits among belt regions |
| Drive-dependent `fuel` for `plsdv` / `arkeng` using `heliu3` | High-Isp logistics | already expressible in module `fuel`; verify `Moving` consumes it on space hops |
| Gas-giant cloud `deutrm`/`heliu3` | Dictionary wants orbit resources on `gasgnt`; until then seed ice-moon surfaces | same as `atmosphere` location-type row above — no extra token |
| `SEE` / scan bonus from `survsc` | Anomaly gameplay | `SeeOrder` + module flag |
| Sick-bay heal cadence (`sckbay`) | Convert wounded crew on a timer so a ward actually saves people; medicines speed the ward without double-taxing the weekly `wndtrn` `medici` bill | Module `<effect type="heal" value="2" target="wndtrn" weeks="4" weeks-with-item="1" with-item-value="4" consume-item="medici" consume-quantity="1"/>`. **Per module quantity.** Each week, **before** race `medici` deduct and **before** week-13 25/50/25: if `medici` ≥ 1, convert up to 4 `wndtrn` → `terran` and consume 1 `medici` per conversion (not per occupied bed-week); a medici-funded week does **not** also tick the unmedicated clock. Else increment a 4-week clock and convert 2 with no item. Remaining `wndtrn` then pay weekly `medici` and, on week 13, roll death/stay/recover. Do not convert `madtrn`. Ignore `crwqrt` 0.1 and `medfac` 1.0 stacked heal until this lands |
| `use-allowed-in module="sckbay"` | Shipboard pharmacy runs in the sterile ward, not in any habitat (`crwqrt`, `jail`) | Extra attr on `use-allowed-in` / `operation-allowed-in`: `module` = module type id. Until then campaign `pharms` uses live `module-type-group="habitat"` (too broad, still loads) |
| Typed weapon group vs matching defence | Laser vs shield, drone vs EW, missile vs point-blank PD, kinetic vs armour. Same four groups at capital / fighter-drone / infantry-tank. Today Battle is flat `attack`/`defense`/`damage` (`player/battle.md`) | Catalog attrs e.g. `weapon-group="laser"` `resists="shield"` on modules/items; `Battle` matchup table. **Do not implement in this designer pass.** |
| Item `attack`/`damage`/`defense` in battle | Live `rctlnc` (and campaign `prllsr` `prlgun` `psnarm` `psnshd` `psnew` `prxgrd`) must affect to-hit and shot damage. Today `ModuleStack.Attack` ignores items (`player/battle.md`) | Add item stats into `getChance` / shot damage / defense dice |
| Alderson point as loadable object | Points sit in the stellar corona as named in-system bodies (`adpnt` planets in campaign XML). Pair always. Instant jump between peers; in-system travel to the point is weeks | Prefer dedicated `<alderson>` (or keep `planet type="adpnt"`). Load `to-system`/`to-point` (or `peer`). `LoadGalaxy` already has an empty “alderson points” comment |
| `JUMP` order | Ship at an AP corona region jumps to the paired AP. Must carry `jmpdvr` for stable AP or `ujpdvr` for unstable AP. 0-week instant transit. Consumes fuel on the jump module. No FTL except through points | `OrdersReader` + move to peer region. Prereq check: module group `propulsion`, module id `jmpdvr` (stable) or `ujpdvr` (unstable). If AP has `unstable="true"`, require `ujpdvr`. Fuel consumption on the module (see tech tree) |
| `unstable` attr on Alderson point | AP `planet type="adpnt"` can carry `unstable="true"`. Unstable points require `ujpdvr` module (L5) instead of `jmpdvr` (L3). Currently ignored by loader (unknown attr). Gameplay: Cinder↔Shards UAP (`A00017`↔`A00018`) bypasses both chokepoints but requires L5 tech | `LoadGalaxy` reads `unstable` attr on `adpnt` planets. `JUMP` order checks: if `unstable`, require `ujpdvr`; else require `jmpdvr` |
| Deep-space transit retires chokepoints | L10 ark / AU×drive can cross Helios–empty without Cinder/Shards (52+ weeks). Until then Cinder and Shards remain mandatory AP gates | Same as AU×drive row; treat non-jump interstellar as the choke bypass |
| `PROSPECT` order (belt survey) | Stack at an `abelt` region with a `prspsc` (prospector scanner) module reveals hidden resource quantities. Without it, belt region resources show as "traces detected" — actual values hidden until prospected. Gameplay: commit scouts before committing miners | `OrdersReader` + region flag `prospected-by` per faction. Until prospected, `SEE` report omits resource `quantity` for that faction. Module prereq `prspsc` |
| Belt region space exits (enhance) | Intra-belt space exits: `abelt` regions within the same planet should have short-duration space exits (1–4 weeks) connecting adjacent rocks. Currently no exits exist between belt regions. Also: planet-orbit ↔ belt-region space exits for approach from orbit | `LoadGalaxy` auto-generates intra-belt exits for `abelt` planets; or campaign XML emits them manually |
| `TOW` order (asteroid towing) | Stack with `towmod` (tow module) at a belt region can `TOW` the region (asteroid) to a destination orbit. Creates a new `abelt` planet at the destination system/AU with the towed region's resources. Travel time = mass × distance / tow-thrust (months to years). Stack is immobile during tow. Fuel consumed continuously | `OrdersReader` new order. `TOW region destination-orbit AU` — creates a `Moving` with the asteroid region attached. On arrival, spawn a new planet with one region. Remove the original region from the source belt |
| `IMPACT` order (kinetic bombardment) | Stack with `impgde` (impact guidance) module at a towed asteroid can target a planet region. Asteroid accelerates toward the target over N weeks (visible to all factions in SEE). On arrival: destroys/damages target region resources, capacity, and stacks. Interception window: defenders can `ATTACK` the tow stack during approach. Military branch L8–L9 | `OrdersReader` + `Events` pipeline for timed impact. Damage formula: asteroid mass × velocity → region resource reduction, stack casualties, capacity loss. Defender interception: if tow stack destroyed, asteroid drifts harmlessly |
| Finite resource depletion | Region resources are finite — extraction reduces `quantity` toward zero. When depleted, the resource row remains at `quantity="0"`. Already implicit in the model (resources have quantities), but verify: `USE` extraction techs should decrement the region resource, not just produce infinitely. Campaign XML seeds finite quantities | Verify `UseOrder` / extraction decrements `Region.Resources[type].Quantity`. If not, add deduction per USE cycle |
| `COUNTER-TOW` order (asteroid interception) | Defender stack with `towmod` at the inbound asteroid's current region can `COUNTER-TOW` to cancel or redirect the `IMPACT`. Requires matching or exceeding the attacker's tow thrust. If successful, asteroid trajectory neutralised (enters harmless orbit or drifts). Both stacks remain immobile during the tug-of-war | `OrdersReader` — same thrust model as `TOW`. If counter-tow thrust ≥ attack thrust, impact cancelled. If less, impact delayed proportionally. Both stacks locked for the duration |
| Destroying inbound asteroid with weapons | Sufficient military firepower can fragment an approaching asteroid before impact. Stack at the asteroid region fires normally; if asteroid "health" (mass-based HP) reaches zero, it shatters into harmless debris. Fragments may seed minor resource deposits in local orbit. Visible approach gives defenders weeks to intercept. Requires no special module — any armed stack can shoot | Asteroid region gets `health` = f(mass). Military `ATTACK` at the region targets the asteroid body. On destruction, cancel `IMPACT` event; optionally spawn a small `smcast`/`smmast` debris region in local orbit |
| Gas-giant orbit regions for skimmer/platform | `gasgnt` planets need orbit regions (type `orbit`) and optional cloud-deck regions (type `atmosphere`) so modules can operate there. Orbit region is the approach point; `atmosphere` region requires `location-type` `atmosphere` (already on wishlist). Skimmer module (`atskmr`) operates in orbit, dips into cloud deck autonomously. Cloud platform (`cldplt`) sits in `atmosphere` region permanently | Add 1–2 regions per `gasgnt` planet: orbit (loadable today as `type="orbit"`) and atmosphere (needs `location-type` `atmosphere`). Skimmer operates from orbit with extraction USE; platform operates from atmosphere region |

| Dual resource model (renewable + finite) | Regions hold both renewable yields (geology/biology — iron veins, soil, aquifers; extraction produces up to N units/turn, never depletes, multiple drills share the cap) and finite deposits (one-time bonanzas — meteorite impacts, alien wreckage, asteroid captures; extraction consumes from the deposit until gone; seeded by GM events, contract rewards, or `PROSPECT` discoveries). Until engine supports `renewable` attr, treat all current region resources as renewable yields. Finite deposits added via GM events | `<resource type="iron" quantity="20" renewable="true" />` for renewable yield cap. `<resource type="iron" quantity="200" renewable="false" />` for finite deposit. Loader reads `renewable` attr (default `true` for backwards compat). Extraction deducts finite; renewable resets each quarter. Design rule: L0–L3 resources renewable everywhere; L4+ resources renewable on moons/gas giants, finite on planets/asteroids |
| Module resource upkeep | Production and habitat modules consume maintenance materials each quarter beyond cash. `factry` +2 `iron`/quarter; `cdrill` +1 `iron`/quarter; `farms` +2 `water`/quarter; `sshull` +1 `iron`/quarter; `city` +5 `iron`/quarter +3 `silici`/quarter; `crwqrt` +1 `food`/quarter per 5 crew. Until implemented, cash upkeep is the only drain | `<upkeep type="iron" quantity="2" period="quarter" />` on module definition. Loader adds periodic resource consume alongside cash. Quarter = week 13 settle |

| `SURVEY` order (body scanning) | Stack with research module at a body (planet/moon orbit or surface) scans for hidden anomalies. Cost: 1 RP per region on the body (e.g., 10-region moon = 10 RP total). Research modules contribute `research-output` per week. `survsc` module doubles RP contribution. Multiple modules stack additively. On completion, all hidden wreckage stacks on that body become visible to the surveying faction. Progress is per-faction | `OrdersReader` + new `SurveyProgress` per faction per planet/moon. Each week: sum research-output of co-located modules (×2 if `survsc` present) → increment progress. When progress ≥ region_count, reveal all faction-1 stacks on that body to the faction |
| `radiation-damage` region attribute | Regions can carry `radiation-damage="N"`. Stacks without sufficient shield rating (`capshd`/`lgshld`/`arkshd`) take N damage/week distributed across modules. Shield modules negate damage up to their rating. Drone crew (`wrkdrn`) items are immune to radiation damage | `LoadGalaxy` reads `radiation-damage` attr on regions. Weekly damage step: if shield_rating < radiation_damage, apply (radiation_damage - shield_rating) to random modules. `wrkdrn` items never take radiation casualties |
| Stellar close-orbit regions (0.1 AU) | Each star gets a 0.1 AU orbit region with `radiation-damage="20"`. Accessible via space exit from the innermost planet (8–13 wk). Construction possible only with drone crew or shielded stacks. Enables late-game solar energy infrastructure | Add 1 region per star (type `orbit`, `radiation-damage="20"`). Space exit from nearest planet arrival. `operation-allowed-in location-type="orbit"` already covers modules operating there |
| Drone crew item type (`wrkdrn`) | Items functioning as crew replacements. Require energy (from energy modules) instead of food/terair. Cannot be wounded — only destroyed (binary operational/scrapped). No morale, no medical, no officer training. Lower initiative than human crew. Immune to radiation and vacuum. Produced by `drnfac` module consuming 2 `silici` + 1 `copper` + 5 energy | New item subtype `crew-drone`. `ModuleStack` crew count includes `wrkdrn` items. Weekly: consume energy per drone (from `energs`/`solgen`); if insufficient energy, drones go offline (0 contribution). Battle: drones destroyed (not wounded) on hit. Initiative penalty: -2 vs human crew |
| Energy beaming (`lsrtx`/`lsrrx` module pair) | Transmitter module at one region beams energy to a receiver module at another region in the same system. Receiver produces energy equal to transmitter output minus distance loss. Enables solar tap energy at 0.1 AU to power operations elsewhere | Paired modules linked by `<link partner="stackid"/>`. Weekly: transmitter energy output → receiver energy input (loss = 5% per AU distance). If transmitter stack destroyed/moved, receiver gets nothing |
| Contract reward types (expanded) | Beyond current `give-module` and `research` triggers, contracts need reward types: `technology` (grant tech), `technology-hint` (reduce cost), `deposit` (seed finite resource), `item` (give items), `skill-unlock` (add to Known Skills), `effect` (unique) | `Contract.Complete()` handler dispatches by reward type. Each type modifies faction state on completion |

| `LAUNCH` order (LRM fire) | Stack with `lrmmod`/`lrmhvy`/`lrmark` at an AP orbit or orbit fires LRM at target (planet region, orbit, specific stack). Creates autonomous unit that travels at module speed. On arrival: if target is planet → random region hit; if target is region → damage split across stacks; if target is immobile stack → direct hit. Moving targets auto-miss. Visible to all during transit. Can be attacked/destroyed en route | `OrdersReader` + autonomous moving entity. Arrival event triggers damage. Multi-warhead: each warhead is a separate entity |
| `DEPLOY` order (mine laying) | Stack with mine-layer module deploys mine items from cargo at current location. Mines become invisible objects at that orbit/region. Multiple mines stack. Trigger: enemy unit enters location → mines detonate (minimum needed to destroy). IFF: smart mines skip friendly. Detonation removes the mine item | `OrdersReader`. Mine items become location-attached invisible objects. Entry trigger calculates required mines to kill entering stack (HP-based). Proportional detonation prevents clearing fields cheaply |
| Detection/visibility model | Default: see units at same location only. Cannot see nested stacks. Detection modules grant visibility radius (AU-based). At each AU tier, units within range are revealed in SEE reports. Detection vs cloaking: higher-tier detection reveals lower-tier cloaking. Same-tier cloaking blocks same-tier detection. Mines are revealed by detection at their location tier | `SeeOrder` / report generation. Module `visibility-AU` attr. Cloaking module `cloak-tier` vs detection module `detect-tier`. Comparison: reveal if detect-tier > cloak-tier. Nested stack visibility requires L2+ detection |
| AP-spanning detection | L8 `apsns` module reveals units in neighbouring system's AP orbit (the paired AP). Combined with LRM, enables strategic first strikes from one system into another. Only reveals AP orbit contents, not deep-system | Detection range extends through AP pairs. Report shows units at peer AP orbit |

When TDD lands a row, tick it here with engine version and date, then migrate any parked catalog lines from `designer/catalog.md` into `campaign/data.xml`.
