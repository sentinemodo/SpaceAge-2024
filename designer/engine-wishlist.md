# Engine wishlist (game designer → TDD)

Designer does **not** implement these. TDD adds a failing test first. Campaign XML stays loadable without them.

**Already live — do not re-wishlist:** `EMoveMode.naval` (catalog `<move mode="naval">`, `<exitmode mode="naval">`; same-body MOVE uses naval duration when stack and exit both have naval; missing mode still defaults to ground). **Race on planet/moon** (`LoadGalaxy` `<race>` on `<planet>`/`<moon>`; orbit `<race>` still loads as fallback; `Orbit.HasAtmosphere` is true for parent `atmosphere="terair"` or body races). **First-class `<belt>`** (composition + MOVE-to-belt; no orbit; no child regions at t=1; rings are belts on gas giants). **First-class `<alderson>`** (orbit, no corona region, no region hops; `JUMP` 1 week to the paired Gate’s orbit, ships only). **Shuttle `h2o2` launch/land surcharge** and **frigate+ land ban** on atmospheres (`MoveOrder` + `BodyEnvironment`; omitted-attr SampleGame worlds stay uncharged and landable) — engine **0.1.148**, 2026-08-29. **Space transit from ΔAU × drive `speed`** (`SpaceTransit` + `MoveOrder`; same-body surface↔orbit stays 1 week; baked region/belt space exits still use exit duration) — engine **0.1.148**, 2026-08-29. Live `f` is still saturating `8 + 6 · (ΔAU / (ΔAU + 0.8))` (default Gate ~14). **Accepted `f(ΔAU)` 2/6/13/39 is pending TDD** (row below). **Mass/thrust on space MOVE** (`effectiveSpeed = catalogSpeed × clamp(load / (40000/4150), 0.67, 1.50)`) — engine **0.1.148**, 2026-08-29. **Rejected:** coastal region type (port = naval exit onto adjacent land; ships occupy that land tile and sail back to water; they cannot walk inland; trucks cannot enter water).

| Need | Objective | Suggested surface |
|------|-----------|-------------------|
| Moon constructed with moon `@name`, not planet name | Unique moon ids (`M00001`) survive load | `DataFile.LoadGalaxy` moon constructor |
| `loadGalaxyExits` walks moon regions and orbits | Moon maps and orbit↔surface space hops. Campaign currently uses **planet-region ↔ planet-region** space exits (Arbor/Anvil spaceports ↔ local dust/belt ↔ the pair hop) because those load today | Second pass over all `Region` / `Orbit` |
| ~~Space transit time from ΔAU × drive `speed`~~ | **Live 0.1.148 (2026-08-29).** Intra-system orbit↔orbit and parent-differing region↔orbit use `ceil(f(\|ΔAU\|) / spaceSpeed)`. Same-body surface↔orbit stays 1 week. Explicit space exits (region↔region, region↔belt) still divide baked duration by speed. Designer may later drop baked 8/13/26 hops. Live `f` is still saturating (default 2 / 13 / 14 / 14). | `SpaceTransit.DurationWeeks` + `MoveOrder.movementDuration` |
| ~~Retune `SpaceTransit` `f(ΔAU)` to 2/6/13/39~~ | **Live 0.1.148 (2026-08-29).** Default workshop frigate: moon 0.04 → **2**, belt 1.7 → **6**, gas 4.2 → **13**, Gate 79 → **39**. Expression in [`au-transit.md`](au-transit.md). Mass clamp 0.67–1.50 unchanged. | `SpaceTransit.DurationWeeks` |
| ~~System `X Y Z` loaded~~ | **Live (2026-08-29).** `LoadGalaxy` assigns system coordinates; save + `SpaceSystem.ReportName` emit them. Star XYZ still commented (optional). No FTL from XYZ. | Uncomment coordinate assign |
| Module group `capital` (or `ark`) | L10 hull is not a “frigate” | `EModuleTypesGroup` + `getModuleTypeGroup` |
| ~~Contract trigger: `destroy-stack`~~ | **Live.** Target stack destroyed in combat; `killer` faction wins. | `DestroyStackTrigger` + `Contract.All.NotifyStackDestroyed` from `Battle` |
| ~~Contract reward: `reward-type="cash"`~~ | **Live.** Credits winner bank on completion. | `Contract.RewardCash` + `Bank.Credit` |
| ~~Module `living-unit="yes"` report labels~~ | **Live.** Wounded / heavily wounded / routed / slain in battle reports for infantry + fauna; mechanics unchanged. | `ModuleType.LivingUnit`; `Module.ReportDamage` / `ReportActive` |
| Contract triggers: `survive-weeks`, `region-resource-below` | Bombardment, pirate hunt | `IContractTrigger` + XML attrs |
| `Events` pipeline for timed spawns | Alien reactivation, impact week | `Game/Events.cs` (today stub) |
| ~~`use-produce effect` execution~~ | **Live 0.1.151 (2026-08-30).** `[repair]` runs via `ProducingEffect` / `UseOrder` (`module-damage` −1 HP on parent scope). `REPAIR` order unchanged. | `UseOrder` / `EProductionType.Effects` |
| Skill children (`usable-in`, cure-chance) applied in battle/medical | **Live 0.1.155 (2026-08-30).** `usable-in` gating + flat combat attrs; `produce` (`effective attack` / `effective defence` × units, `research output` per officer on labs); `cure-chance` on quarterly wounded outcome. | `SkillType` fill-pass; `Tests/TSkill.cs`, `Tests/TConsume.cs`, `Tests/TResearch.cs` |
| Item `radiation` / equipment bonuses in combat and vacuum | Vests, suits, dosimeters | `ItemType` + consume/medical |
| ~~New `location-type` `atmosphere`~~ | **Live 0.1.158 (2026-09-09).** `LoadLocationType("atmosphere")`; gas-giant orbit with parent `atmosphere` ≠ `none` → `BodyEnvironment.EffectiveLocationType` = `atmosphere`. | `LoadLocationType` + `BodyEnvironment` |
| Drive-dependent `fuel` for `fustor` / `plsdv` / `arkeng` using `heliu3` | L2 torch is **2 `heliu3` / week**; later pulse/ark same item | already expressible in module `fuel`; verify `Moving` consumes it on space hops |
| ~~Gas-giant cloud `deutrm`/`heliu3`~~ | **Live 0.1.158 (2026-09-09).** Orbit `<resource>` load/save; campaign seeds pockets on every `gasgnt` orbit; `ramsco` / `he3skm` / `d2skm` in `campaign/data.xml`. Ice-moon pockets remain for early paths. | `LoadGalaxy` orbit resources + atmosphere gates |
| `SEE` / scan bonus from `survsc` | Anomaly gameplay | `SeeOrder` + module flag |
| ~~Sick-bay heal cadence (`sckbay`)~~ | **Live 0.1.150 (2026-08-30).** `ExecuteSickBayHeal` weekly before `ExecuteMedicalConsume`; week-13 heal in the 13-week loop before quarterly 25/50/25. Medici path: up to 4 `wndtrn`→`terran` per bay per week, 1 `medici` per conversion; unmedicated: 2 per bay every 4 weeks (`sick-bay-weeks` persisted). `medfac`/`crwqrt` stacked heal still not executed. | `ModuleStack.Upkeep.cs` + `Tests/TConsume.cs` |
| ~~`use-allowed-in module="sckbay"`~~ | **Live 0.1.150 (2026-08-30).** `CatalogLoader` + `UseOrder` gate `pharms` to module type `sckbay`. | `UseOrder.UseCondition_ModuleType` |
| ~~Typed weapon group vs matching defence~~ | **Live.** `CatalogLoader` reads `weapon-group`/`resists`; `CombatMatchup.ChanceMultiplier` + `Battle.getChance`. SampleGame omits attrs → flat dice. | Load attrs; `Battle` matchup table |
| ~~Armour module hit bias + no capture~~ | **Live.** `hitWeight` ×5; capture HP/pool ignored on armour (`CombatMatchup.IsArmor`). | `Battle.hitWeight` and capture completion path |
| ~~Shield damage intercept~~ | **Live.** `CombatMatchup.ShieldIntercept` floor 90% onto a shield module; remainder uses normal hit-weight. | Pre-allocation step in shot resolution |
| ~~Hull size groups beyond frigate~~ | **Live 2026-08-30.** Parse/ToToken/`IsShipHull` for `corvette` `destroyer` `cruiser` `capital` `ark`; campaign catalog retagged; allow-lists expanded. SampleGame stays `frigate`. | `EModuleTypesGroup` + catalog `group=` (see `designer/combat-balance.md`) |
| ~~Item `attack`/`damage`/`defense` in battle~~ | **Live 0.1.149 (2026-08-30).** Cargo equipment with matching `use-allowed-by module-type-group` adds `attack`/`defense` to stack dice (`min(item qty, QuantityActive)` per type) and `damage` on first boosted shots per volley. `defense`/`initiative` loaded on `ItemType`. | `ModuleStack.Attack`/`Defense`; `Battle` shot damage |
| ~~Body `gravity` / `temperature` / `atmosphere` attrs~~ | **Live 0.1.148 (2026-08-29).** `LoadGalaxy` reads bands on planet/moon; defaults gravity normal, temperature cold, atmosphere none. See `designer/environments.md` | `LoadGalaxy` + `BodyEnvironment` |
| ~~Shuttle launch/land fuel surcharge~~ | **Live 0.1.148 (2026-08-29).** Fixed integer `h2o2` cost on surface↔orbit moves by gravity×atmosphere table (not mass-fraction). Omitted-attr bodies charge 0 | `MoveOrder` consumes surcharge from stack cargo; fail if insufficient |
| ~~Spaceship land ban on atmospheres~~ | **Live 0.1.148 (2026-08-29).** Frigate+ hulls cannot use solid-surface exits if body `atmosphere` ≠ `none`. Shuttles still ferry. Omitted-attr worlds are not banned | `MoveOrder` + `BodyEnvironment.BansNonShuttleSurfaceHop` |
| ~~High-gravity cash upkeep~~ | **Live 0.1.148 (2026-08-29).** +50% cash upkeep (round up) on `gravity="high"`; +2 food/quarter on populated habitats | Quarterly upkeep step reads body gravity |
| ~~Temperature settlement gates~~ | **Live 0.1.148 (2026-08-29).** `cold` needs `clddom`/`cryhab`; `hot` needs `hotdom`; `habitable` uses normal cities | Settlement USE / city place checks body temperature vs module type |
| Multi-reward contracts | Hostile faction bounty: cash **and** technology on one completion (UN pay + finders keepers) | `Contract.Complete` applies multiple `<reward>` children; or `reward-type` list |
| ~~Item nominal `value`~~ | **Live 0.1.150 (2026-08-30).** `CatalogLoader` reads item/module/race `value`; `Market.GetPrice` falls back when no region has posted a price. Campaign trade ladder in [`economy.md`](economy.md); SampleGame omits attrs → 0. | Load item/module `value`; `GetPrice` falls back to it. Design ladder: [`economy.md`](economy.md) |
| Capture stack trigger | UN charter to capture Arbor First / HCS city or HQ stack | `trigger="capture-stack"` + `target` stack id; optional hold-region weeks |
| Hostility flip on first spaceship | Arbor First (12) / HCS (13) go `DECLARE DEFAULT ENEMY` when `shuttl` or `frigate`/`spacecraft` production completes on that planet | `UseOrder`/`Produce` complete hook; planet of location; once-per-planet flag on faction or game |
| Militia yearly raid | After flip, each year spawn/release 3 `inftry` toward a random player HQ on that planet | `Events` pipeline or GM `order.*` for factions 12/13; stop when city captured |
| ~~Reveal body flavour on `RESEARCH` space-object + T1 home blurbs~~ | **Live 0.1.148 (2026-08-30).** Campaign `description=` on galaxy bodies reaches `"Survey reports:"` like tech blurbs. `RESEARCH <space-object-id>` requires proximity (lab in target orbit/region/belt, or on the body's orbit/surface); **stars are exempt**. Turn-1 `/reports` seeds home star + planet for factions 2–11. `ObjectsSeen` persists on save. | `Research.TryRevealSpaceObject`; `SurveyObjects` / `SurveyReports`; `ReportWriter` survey block |
| ~~Mass/thrust factor on space MOVE duration~~ | **Live 0.1.148 (2026-08-29).** Default workshop frigate (mass **4150**, one `fustor` thrust **40000**) keeps the AU table (after `f` retune: **2 / 6 / 13 / 39**, not 2 / 13 / 14 / 14). Scout clamp MAX **1.50**, same-engine cargo MIN **0.67**. Same-body surface↔orbit stays 1 week. Baked space exits use the factor. Zero thrust → factor 1. | `SpaceTransit.MassFactor` + `EffectiveSpaceSpeed`; locked ships + table: [`au-transit.md`](au-transit.md) |
| ~~Region `<anomaly>` load + exit hint in reports~~ | **Live 0.1.160 (2026-09-13).** `<anomaly type="…">` on `<region>` loads to `Region.AnomalyType` (legacy `<poi>` alias on load); visible source region exit lines toward an anomaly cell append `, anomaly detected`. Full `description` not shown on exit hint. | `Galaxy.loadAnomaly`; `Exits.Report(faction, fromRegion)`; `Tests/TReport.cs` |
| ~~Region `<deep-pocket>` load + cdrill-gated hints~~ | **Live 0.1.163 (2026-09-13).** `<deep-pocket>` child on `<region>`; exit hint `, deep pocket of resources detected` when source region holds `cdrill` tech/module; `Deep resources:` line only with `cdrill` in pocket region. Campaign seed: `HQ_DEEP_POCKETS` (~10/71). | `Galaxy.loadDeepPocket`; `Region.HasCdrillTechnologyFor`; `Exits.Report`; `Tests/TReport.cs` |
| ~~Settlement exit hint from visible grant~~ | **Live 0.1.165 (2026-09-14).** Neighbouring region with any settlement-group stack (`town`, `city`, `mtrply`, dome variants) appends `, settlement detected` on exit lines from a visible source region. | `Region.HasSettlement`; `Exits.Report`; `Tests/TReport.cs` |
| ~~Fauna settlement-adjacent rumors~~ | **Live 0.1.167 (2026-09-14).** Fauna factions 14–17: when a stack is in a region neighbouring a settlement, auto-issue anonymous planet rumor with stack location (`FaunaRumors.IssueAll` before reports and in `Events.Execute`). | `Game/FaunaRumors.cs`; `Tests/TFaunaRumors.cs` |
| ~~Fractional `research-output` (`moblab`)~~ | **Live 0.1.160 (2026-09-13).** Optional `research-output-divisor` on modules; `Research.WeeklyOutput` accrues remainder on stack (`research-output-remainder` on save). Campaign `moblab` uses divisor **2** (half `cmplib` throughput). | `ModuleType.ResearchOutputDivisor`; `Tests/TResearch.cs` |
| ~~`RESEARCH` on anomaly region~~ | **Live 0.1.162 (2026-09-13).** `RESEARCH <region-id>` when region has unresolved `<anomaly>`; weekly progress from `investigation-output` modules + `research-throughput` items. Design: [`anomaly-investigation.md`](anomaly-investigation.md) | `ResearchOrder.researchAnomaly`; `EResearchType.Anomaly` |
| ~~Anomaly progress / resolved state on region~~ | **Live 0.1.162.** `<progress>` / `<resolved>` under `<anomaly>` on save; `<reward>` children from gamein | `RegionAnomaly`; `Galaxy.loadAnomaly` |
| ~~Type-matched investigation throughput~~ | **Live 0.1.162.** `investigation-output` + `investigation-types` on modules; default match table for labs without explicit types | `AnomalyInvestigation.WeeklyProgress`; `CatalogLoader` |
| ~~Investigation payout dispatcher~~ | **Live 0.1.162.** On resolve: survey blurb, RP toward named tech, resource reveal, tech copy by faction band | `AnomalyInvestigation.Complete` |
| **Re-open anomaly at higher tech band** (optional) | Resolved L0–1 HQ site grants L4+ payout when revisited with matched lab | Second threshold or `investigation-band` on region |
| Settlement buy-book fulfillment → garrison spawn | When an NPC `town`/`city`/`mtrply` stack has **filled** its standing auto-buy item targets (food + ore intake delivered this quarter or cumulatively), spawn nested **`inftry`** and static **`gunbot`** / **`pdltur`** placements under that settlement | `GenerateOffers` post-pass or `Events` at week 13; cap by settlement tier; stop when city captured |
| Planet tech band → autobuy resource list | Expand settlement auto-buy item types based on **highest technology level researched on that planet** (any faction lab in any region/orbit of the body): L0 food/iron/carbon; L1 +oil/silici; L2 +titani/copper; L3 +uraniu/heliu3; L4+ strategic exotics | `SettlementBuyBook` reads planet-scoped tech ceiling; merge with tier table in [`economy.md`](economy.md) |
| Nested factory → auto-sell produced modules | When a settlement stack nests an operational **`factry`**, quarter-end **`GenerateOffers`** lists **one** produced module type the factory can build (prioritize cargo, energy, farms, drill) as **`selling module=`** at catalog bid × ask spread | Requires factory output catalog lookup; skip if sell already standing |
| Fauna quarterly growth | Tier 1/2/3 breed and promote per [`fauna.md`](fauna.md) at week 13 | `Events` pipeline or GM; spawn native module types by body |
| Fauna quarterly movement | Random solid-neighbour MOVE; split when ≥2 apex units | Same pipeline; respect water region types |
| Fauna battle loot | Item drops + RP split among attacker stacks on destroy | `Battle` post-wreck hook; table in `fauna.md` |
| Fauna pen + tame breeding | L3 techs (`brdtam`, `crstam`, `havbre`, `grftam`) produce tame variants (`tbrmst`, …) from captive wild stacks or samples at a fauna pen module | `use-produce module=` from pen; pen accepts captured fauna stack or tissue item; tame modules get `can-convert="yes"` |
| Wild fauna faction AI | Factions 14–17 roam/forage without player orders | GM orders or `Events` pipeline for fauna upkeep |

When TDD lands a row, tick it here with engine version and date, then migrate any parked catalog lines from `designer/catalog.md` into `campaign/data.xml`.
